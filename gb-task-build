#!/bin/sh -efu

. gb-sh-tmpdir

for arch in i586 x86_64; do
	$0-arch "$arch" ||
		echo "$arch build failed" >>"$tmpdir"/FAIL &
	sleep 9
done

wait

if [ -s "$tmpdir"/FAIL ]; then
	cat >&2 "$tmpdir"/FAIL
	exit 1
fi

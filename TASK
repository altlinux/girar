Define task directory structure.

# When the task is first created.
TASK_ID/task/id			TASK_ID==id
TASK_ID/task/owner		GIRAR_USER
TASK_ID/task/repo		Sisyphus or 4.0 or 4.1

TASK_ID/task/log		important task events

# Task "has-a" list of gear repositories for build.
TASK_ID/gears/$i/dir		/people/GIRAR_USER/packages/${dir##*/}.git
TASK_ID/gears/$i/tag_name	1.0-alt1
TASK_ID/gears/$i/tag_value	SHA1 hex

# For each gear repo, we first build pkg.tar.
TASK_ID/gears/$i/pkg.tar

# Build results build/$i/ is 1-1 to gears/$i/, with per-arch subdirs.
TASK_ID/gears/build/$i/$arch/chroot_base	hsh --initroot packages
TASK_ID/gears/build/$i/$arch/chroot_BR		BuildRequires packages

# Packages built with hasher.
TASK_ID/gears/build/$i/$arch/srpm/*.src.rpm	exactly one *.src.rpm
TASK_ID/gears/build/$i/$arch/rpms/*.rpm		1+ *.$arch.rpm or *.noarch.rpm

# After the build is successful, there is a palan to upgrade the repo.
TASK_ID/paln/add-src
TASK_ID/paln/add-bin
TASK_ID/paln/rm-src
TASK_ID/paln/rm-bin

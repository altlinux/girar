#!/bin/sh -e

export IFS=' 	
'
export PATH=/sbin:/usr/sbin:/bin:/usr/bin

. "@CMD_DIR@/girar-sh-functions"

usage()
{
	echo "$PROG: $*" >&2
	echo "usage: $PROG <NAME>"
	exit 1
}

set_name "$1"
shift

IT_NAME="@USER_PREFIX@$NAME"

gpasswd -d "$IT_NAME" "@TASKS_GROUP@" ||
	fatal "error disabling girar build permissions for user $IT_NAME"

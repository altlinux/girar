#!/bin/sh -efu

. gb-sh-setup

id="$1" arch="$2"; shift 2
cd "$GB_TASKS/$id"

gear_nums()
{
	set +f
	cd gears &&
	ls -1 [1-9]*/dir |sort -n |cut -d/ -f1
}

mkdir -p "$hsh_dir"
rm -rf "$hsh_dir"/repo

nums=$(gear_nums)

for i in $nums; do
	$0-i "$id" "$arch" "$i"
done

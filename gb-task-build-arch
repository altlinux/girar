#!/bin/sh -efu

. gb-sh-hasher-conf

arch="$1"; shift

hsh_dir=$(gb_hasher_dir "$arch")
rm -rf "$hsh_dir"/repo

gear_nums()
{
	set +f
	cd gears &&
	ls -1 [1-9]*/dir |sort -n |cut -d/ -f1
}

nums=$(gear_nums)

for i in $nums; do
	$0-i "$arch" "$i"
done

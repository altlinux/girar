#!/bin/sh -efu

. shell-quote

arch="$1"; shift

. gb-sh-remote-conf

ssh "$remote_host" ./gb-remote-plant \
	"\"$(quote_shell "$GB_REPO_DIR")\"" "\"$(quote_shell "$arch")\""

. gb-sh-nums

for i in $(gear_nums); do
	$0-i "$arch" "$i"
done
